# Kubernetes Infrastructure as Code

This components declaratively configures the kubernetes clusters using
terraform.

## Structure

### Modules

Reusable terraform blocks to be consumed by environments.

### Environments

Each cluster configuration.
